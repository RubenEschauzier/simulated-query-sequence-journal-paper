\textbf{Journal extension}
\begin{todolist}
    \item Start micro benchmark experiment for disk-based and quad-store (After the other experiments finish running)
    \item Extend same results we already have with disk, offline traversal, and quad store
    \item Include refinement pattern analysis (generate sequence with one query that is refined) investigate
    impact of certain chokepoints on performance
    \item Include hyperparameter search where we investigate effect of different hyperparameter settings on 
    data locality (how much of the data is overlapping between queries))
\end{todolist}

\section{Introduction}

Emerging web applications increasingly rely on decentralized architectures to give users control over their personal data.
Structured decentralized ecosystems, such as Solid~\cite{sambra2016solid} and Mastodon~\cite{raman2019challenges},
encourage users to keep their data in small, independent stores following shared data models.
This paradigm shift is increasingly vital for automated systems, such as LLM-based AI agents,
which query these decentralized structures to extract facts for task performance or question answering~\cite{walter2025grasp,karki2026agentic,bernerslee2017charlie}.
While this approach strengthens user autonomy, the absence of a central data authority naturally creates a highly multi-perspective environment. 
Data spreads across thousands of heterogeneous sources that frequently contain overlapping, diverse, 
or conflicting claims.
Query engines must therefore dynamically fetch and integrate this information at runtime, 
not only to discover conflicting statements and multiple perspectives, 
but to reconcile these claims to provide a coherent answer to the user or agent.

% Emerging web applications increasingly rely on decentralized architectures to give users control over their personal data. 
% Structured decentralized ecosystems, such as Solid~\cite{sambra2016solid} and Mastodon~\cite{raman2019challenges}, 
% encourage users to keep their data in small, independent stores that follow shared data models. 
% While this approach strengthens user autonomy, the absence of a central data authority naturally creates a multi-perspective environment.
% Data spreads across heterogeneous sources that frequently contain overlapping, diverse, or conflicting claims. 
% Query engines must therefore be able to dynamically query this information, 
% to discover conflicting statements and multiple perspectives, and to reconcile these claims to provide a coherent answer to the user.
% This is a notoriously difficult task when engines must navigate thousands of sources, each enforcing local access policies.

% Emerging automated systems increasingly rely on knowledge graphs to ground the reasoning and decision-making of LLM-based AI agents.
% Language models are progressively utilized to interact with~\cite{zahera2024generating} 
% and build~\cite{lippolis2025large, benhocine2022towards} these knowledge bases.
% During execution, these systems issue queries over the knowledge graph to extract relevant facts 
% for task performance or question answering~\cite{walter2025grasp, karki2026agentic}. 
% Similarly, human-in-the-loop engineering pipelines rely on LLMs for curation and maintenance tasks, 
% which in turn necessitates iterative graph querying to inspect existing data structures.

% While these systems typically operate on centralized knowledge bases (such as knowledge graphs),
% emerging web architectures prioritize decentralization to give users control over their data.
% This shift is also seen in LLM-based systems that use knowledge graphs for factual grounding.
% For example, deploying Solid~\cite{sambra2016solid} as a decentralized knowledge graph 
% allows individuals to retain autonomy over their personal data and dictate exactly which information feeds 
% the agent's underlying model~\cite{bernerslee2017charlie}. 

% Structured decentralized ecosystems such as Solid~\cite{sambra2016solid} and Mastodon~\cite{raman2019challenges}
% partition global Knowledge Graphs into small, independent sub-graphs managed by individuals. 
% This approach strengthens user autonomy but complicates data consumption; 
% (LLM-based) automated systems must identify and query many heterogeneous data sources on the fly, each enforcing distinct access policies.  

% Emerging automated systems increasingly rely on knowledge graphs to ground the reasoning and decision-making
% of an LLM-based AI agent. 
% In general, LLM-based systems are increasingly used to interact~\cite{zahera2024generating} 
% and build~\cite{lippolis2025large, benhocine2022towards} knowledge graphs.
% These LLM-based systems execute queries over the knowledge graph to obtain 
% relevant information to answer user questions or perform tasks~\cite{walter2025grasp, karki2026agentic}.
% In addition, human-in-the-loop systems for knowledge graph engineering increasingly rely on LLMs 
% to assist in the curation and maintenance of knowledge graphs,
% which in turn requires executing queries over the knowledge graph to obtain relevant information.

% These systems operate on centralized knowledge graphs, however, emerging web applications increasingly
% rely on decentralized architectures to give users control over their personal data.
% This same pattern is seen in context of LLM systems where a knowledge graph serves as a factual grounding for the LLM.
% For example, using Solid~\cite{sambra2016solid} as a decentralized knowledge graph for LLMs allows 
% users to control their personal data and the information that is used to answer their questions~\cite{bernerslee2017charlie}.

% decentralized architectures to give users control over their data,
% requiring engines to dynamically fetch and integrate knowledge distributed across the web.
% While centralizing data simplifies processing, it conflicts with privacy requirements and access controls.
% Structured decentralized ecosystems such as Solid~\cite{sambra2016solid}  and Mastodon~\cite{raman2019challenges}
% partition global Knowledge Graphs into small, independent sub-graphs managed by individuals. 
% This approach strengthens user autonomy but complicates data consumption; 
% automated systems must identify and query many heterogeneous data sources on the fly, each enforcing distinct access policies.  

% Emerging web applications increasingly rely on decentralized architectures to give users control over their personal data,
% requiring query engines to dynamically fetch and integrate information distributed across the web.
% While centralizing this data simplifies query processing, it often conflicts with privacy requirements 
% and fine-grained access controls.
% Structured decentralized ecosystems such as Solid~\cite{sambra2016solid} and Mastodon~\cite{raman2019challenges} encourage users to keep their own data in small,
% independent stores that follow shared data models. 
% This approach strengthens user autonomy, 
% yet it makes query execution more difficult because engines must identify and query many heterogeneous data sources,
% each with its own access policies.

Traditional federated querying approaches~\cite{schwarte2011fedx,schmidt2011fedbench,heling2022federated} support decentralization of sources,
but typically assume a small (10-100) set of well-described and consistently available endpoints~\cite{dang2023fedshop}. 
These assumptions fail once data is spread across thousands of small units that enforce local access-control policies.
In addition, enforcing fine-grained access control policies~\cite{esteves2021odrl} on large sources with expressive interfaces introduces notable 
overhead~\cite{padia2015attribute}, making standard approaches unsuitable for highly decentralized environments.

Newer decentralized environments, such as Solid, often expose data as lightweight HTTP resources instead of highly expressive query interfaces. 
Identifying and resolving inconsistencies across these networks requires a querying approach
capable of navigating resources that contain conflicting information.
Many of these resources are not known in advance and can only be discovered during query execution. 
While their simplicity lowers the cost of enforcing fine-grained controls,
this setting requires a federation model that can operate over a very large number of small sources,
many of which may be encountered at runtime, without relying on prior aggregation.
Traditional federated approaches are insufficient in this setting due to the assumption of a small number of large endpoints.
Executing SPARQL queries using Link Traversal-based Query Processing (LTQP)~\cite{hartig2009executing} fits these needs.
It discovers documents incrementally by following URIs encountered during execution, 
starting from an initial set of seed documents provided by either the user or the query.
Its link-driven, asynchronous traversal supports fine-grained permissions 
and allows systems to discover and reconcile conflicting information without knowing the location of all decentralized data sources beforehand.

However, the fact that LTQP discovers documents incrementally creates significant challenges for efficient execution. 
Using the traditional optimize-then-execute paradigm is difficult, as the query itself determines which data will be accessed. 
As a result, the optimizer has no prior knowledge of the relevant data until the query’s traversal has already begun.
Some approaches optimize query execution without prior knowledge by using adaptive query processing~\cite{hanski2025link} and zero-knowledge query planning~\cite{hartig2011zero}. 
These yield modest but inconsistent performance gains, depending on the decentralized environment and the heuristics used~\cite{taelman2023link}.
However, other aspects of zero-knowledge LTQP optimization, such as link prioritization~\cite{hartig2016walking},
are unlikely to improve query result arrival times in emerging decentralized environments~\cite{eschauzier2025revisiting}.

Consequently, other works explore using precomputed server-side information as prior knowledge 
or relevance indexes to efficiently execute LTQP queries \newline \cite{tam2024opportunities,umbrich2011comparing,hanski2024observations}.
While these approaches offer greater performance benefits, they require the server to maintain these indexes.
This raises the cost of serving resources and complicates privacy and access control.

To avoid server-side overhead, engines can derive prior knowledge directly on the client.
In decentralized settings, LTQP is inherently client-side, with a single engine instance serving one or more applications.
As these applications issue sequences of correlated queries,
the engine identifies frequently accessed data and its characteristics.
This allows engines to implement \emph{personalized} query optimization algorithms 
that leverage local access patterns to build necessary prior knowledge.


\begin{figure}
	\centering
	\includegraphics[width=.9\columnwidth]{figures/overlapping_queries_fixed.pdf}
	\caption{When users interact with agents, their queries intentions and thus relevant data to the query can overlap, leading to opportunities for personalized optimization.}
	\label{fig:intro-overlap}
\end{figure}
\todo{Adjust figure to reflect fact that AI is no longer focus}

Benchmarking these approaches requires realistic query sequences.
Traditional benchmarks (e.g., WatDiv~\cite{alucc2014diversified}, \\ BSBM~\cite{bizer2011berlin}) 
execute isolated or repeated query templates, which fails to capture client-side dynamics accurately. 
Furthermore, while centralized SPARQL optimization relies on
real query logs~\cite{berendt2013usewod2013,picalausa_what_2011,bonifati_darql_2018,stadler2024lsq}, 
such logs do not yet exist for decentralized LTQP environments. 
Consequently, we need a standardized benchmark that realistically simulates user query sequences 
to prevent fragmented evaluation practices.

In this paper, we address this gap by introducing SolidSessionBench,
a benchmark extending SolidBench~\cite{taelman2023link} to generate realistic query sequences.
These sequences capture usage patterns identified in social network literature, 
a domain naturally rich in multi-perspective data, 
alongside patterns observed in real-world SPARQL logs.

To evaluate the realism of our generated query sequences without access to real-world query logs, 
we investigate \todo{insert} and evaluate \todo{insert} baseline client-side caching strategies
in the LTQP engine Comunica~\cite{taelman2018comunica,taelman2023link}. 
We use these simple algorithms to establish a performance baseline and demonstrate the necessity of sequence-based evaluation.
Comparing this baseline against existing literature shows 
that current evaluation techniques are insufficient for client-side caching in LTQP.

By enabling researchers to evaluate personalized query optimization against actual user behaviors,
SolidSessionBench lays the foundation for performant client-side querying. 
Ultimately, efficient LTQP execution over decentralized sources facilitates the rapid identification of conflicting information 
and the application of consistency fixes across multi-dimensional knowledge graphs.

The remainder of this paper is structured as follows.
Section~\ref{sec:related-work} reviews prior benchmarking approaches and real-world query usage patterns.
Section~\ref{sec:simulation-methodology} outlines our simulation methodology and defines the resulting hyperparameters.
Section~\ref{sec:client-side-caching} introduces the technical mechanics of client-side caching strategies in LTQP.
Section~\ref{sec:evaluation} evaluates the benchmark using proxy measures of realism. 
Finally, Section~\ref{sec:conclusion} concludes the paper and presents a plan for benchmark sustainability.

\todo{Maybe insert section summarizing contributions.}